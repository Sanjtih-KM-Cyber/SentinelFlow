# SentinelFlow Python Dependencies — updated with dashboard
# Install with: pip install -r requirements.txt

# ─── Core Async Framework ─────────────────────────────────────────────────────
aiohttp>=3.9.0
aiofiles>=23.0.0
aiosqlite>=0.19.0

# ─── DNS & Network ────────────────────────────────────────────────────────────
dnspython>=2.4.0
certifi>=2024.0.0

# ─── Configuration ────────────────────────────────────────────────────────────
PyYAML>=6.0.0
python-dotenv>=1.0.0

# ─── Reporting ────────────────────────────────────────────────────────────────
# PDF generation (choose one or both)
reportlab>=4.0.0             # Lightweight PDF generation
# weasyprint>=60.0           # Full HTML→PDF (requires system libs, more complex install)

# ─── Optional: Enhanced Features ──────────────────────────────────────────────
# Uncomment as needed:
# shodan                     # Shodan API client
# censys                     # Censys API client
# python-telegram-bot>=20.0  # Alternative Telegram library (we use raw API)

# ─── Development & Testing ────────────────────────────────────────────────────
pytest>=7.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0

# ─── Desktop App ──────────────────────────────────────────────────────────────
# tkinter is built into Python stdlib (no pip install needed)
pystray>=0.19.0          # System tray icon (Windows/Mac/Linux)
pillow>=10.0.0           # Icon generation + image handling

# ─── Build Standalone Executable ──────────────────────────────────────────────
# Run: python desktop/build.py  → produces dist/SentinelFlow.exe / .app
pyinstaller>=6.0.0
